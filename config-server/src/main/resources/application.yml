server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///Users/shimaazakarya/Task-Project1/config-repo
          default-label: master
          search-paths: config-files
